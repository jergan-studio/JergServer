/**
 * JergServers Configuration Matrix
 */
const Config = {
    // Active game mode settings: 'survival' or 'creative'
    mode: 'survival',
    
    // Anti-Knockoff enforcement toggle (Disconnects unverified clients)
    allowKnockoffs: false 
};

module.exports = Config;
